name: Unit Tests

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  test:
    name: Build and Test
    runs-on: self-hosted
    timeout-minutes: 120
    permissions:
      contents: read
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        submodules: recursive
        
    - name: Setup ccache
      uses: hendrikmuhs/ccache-action@v1.2
      with:
        key: ${{ runner.os }}-ccache
    
    - name: Cache LLVM Build
      uses: actions/cache@v4
      with:
        path: |
          build/x64-debug-linux/llvm
        key: ${{ runner.os }}-llvm-${{ hashFiles('llvm/**') }}
        restore-keys: |
          ${{ runner.os }}-llvm-
    
    - name: Configure CMake
      run: |
        cmake --preset debug-linux
        
    - name: Build Tests
      run: |
        cmake --build build/debug-linux --target vkml-compiler tensor_tests arithmetic_ops_tests bitwise_ops_tests comparison_ops_tests logical_ops_tests other_ops_tests
    
    - name: Run Tests
      run: |
        cd build/x64-debug-linux
        ctest --output-on-failure --verbose
